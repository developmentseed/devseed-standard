# devseed-standard

## Install

```sh
npm i -D devseed-standard
```

## ESlint config file

Create a .eslintrc file:

```json
{
  "extends": ["devseed-standard"]
}
```

## Cli Usage

Add an npm script:

```
"scripts": {
  "lint": "devseed-standard"
},
```

Run the script:

```
npm run lint
```
